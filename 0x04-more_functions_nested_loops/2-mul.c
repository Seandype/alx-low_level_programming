#include "main.h"

/**
 *mul - compuites the multiplication of 2 integers
 *@a: first of the two numbers to be multiplied
 *@b: second of the two numbers to be multiplied
 *Return: The result of the multiplication
 */

int mul(int a, int b)
{
	return (a * b);
}
